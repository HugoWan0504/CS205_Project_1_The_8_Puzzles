#ifndef HEURISTICS_H
#define HEURISTICS_H

#include <bits/stdc++.h>
using namespace std;

// A* Heuristics: AMT and AMD
int misplacedTile(const vector<vector<int>>& state);
int manhattanDistance(const vector<vector<int>>& state);

#endif
