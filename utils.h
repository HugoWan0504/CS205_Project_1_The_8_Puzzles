#ifndef UTILS_H
#define UTILS_H

#include <bits/stdc++.h>
using namespace std;

vector<int> parseRow(const string& input);
bool isUniqueDigitsZeroToEight(const vector<int>& values);

#endif
